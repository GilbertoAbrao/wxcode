"""Routes package.

Generated by wxcode from {{ project_name }}.
"""

from fastapi import APIRouter

{% for route in routes %}
from .{{ route.filename }} import router as {{ route.router_name }}
{% endfor %}

router = APIRouter()

{% for route in routes %}
router.include_router(
    {{ route.router_name }},
    {% if route.prefix %}prefix="{{ route.prefix }}",{% endif %}

    {% if route.tags %}tags={{ route.tags }},{% endif %}

)
{% endfor %}
