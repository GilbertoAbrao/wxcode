"""Pydantic model for {{ table_name }}.

Generated by wxcode from HyperFile schema.
Original table: {{ original_table_name }}
"""

from datetime import date, datetime, time
from decimal import Decimal
from typing import Optional
from uuid import UUID

from pydantic import BaseModel, ConfigDict
{% if has_email_field %}
from pydantic import EmailStr
{% endif %}


class {{ class_name }}(BaseModel):
    """{{ docstring or 'Pydantic model for ' + table_name }}."""

    model_config = ConfigDict(from_attributes=True)

    {% for field in fields %}
    {% if field.comment %}
    # {{ field.comment }}
    {% endif %}
    {{ field.name }}: {{ field.type }}{% if field.default is not none %} = {{ field.default }}{% endif %}

    {% endfor %}
